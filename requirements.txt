# EMBER runtime dependencies.
# Install everything needed to run the app locally with:
#   pip install -r requirements.txt
#
# Python 3.11 or 3.12 is recommended. pip automatically pulls in the smaller
# libraries these depend on, so this list is all you need.

# Web app + interactive map
streamlit==1.59.2
streamlit-folium==0.22.0
folium==0.17.0
matplotlib==3.9.2

# Data catalog (reads the Parquet tables)
duckdb==1.3.2
numpy==1.26.4
PyYAML==6.0.2

# Burn-severity tile service
fastapi==0.129.1
uvicorn==0.30.6
rio-tiler==6.8.0
rasterio==1.4.4

# Google Cloud Storage access (for the hosted data)
gcsfs==2024.6.1
fsspec==2024.6.1
google-cloud-storage==2.18.2

# Settings and TLS certificates
pydantic==2.9.2
pydantic-settings==2.5.2
certifi
